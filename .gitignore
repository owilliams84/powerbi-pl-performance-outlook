# The source dataset is published under "Data files (c) Original Authors" - all rights
# reserved, no redistribution licence - so neither it nor anything derived from it is
# committed. See the README for how to fetch it and rebuild in one command.
data/

# Power BI Desktop writes these next to the PBIP while a project is open. They are per-machine
# state (window layout, cached model data, credentials), not part of the definition.
.pbi/
**/.pbi/
localSettings.json
**/localSettings.json
*.pbix
cache.abf
diagramLayout.json

# Python
__pycache__/
*.py[cod]

# OS
Thumbs.db
desktop.ini
.DS_Store
